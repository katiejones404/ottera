# Ottera Backend

Run locally:

1. cd backend
2. create a .env file with:
   PORT=4000
   SUPABASE_URL=...
   SUPABASE_SERVICE_ROLE_KEY=...
   SUPABASE_ANON_KEY=...
3. npm install
4. npm run dev

Server runs at http://localhost:4000